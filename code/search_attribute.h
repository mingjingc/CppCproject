#ifndef GUARD_search_attribute_h
#define GUARD_search_attribute_h
#include <vector>
#include <string>
#include "Student_and_dorm_Infor.h"
#include "attribute_name.h"

void search_weight(const std::vector<attributeName>& weight_names);
void search_money(const std::vector<attributeName>& money_names);
void search_knowledge(const std::vector<attributeName>& knowledge_names);
void search_charm(const std::vector<attributeName>& charm_names);
void search_health(const std::vector<attributeName>& healtht_names);
void search_looks(const std::vector<attributeName>& looks_names);


#endif // !GUARD_search_attribute_h
